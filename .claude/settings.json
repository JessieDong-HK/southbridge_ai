{
  "permissions": {
    "defaultMode": "dontAsk",
    "allow": [
      "WebFetch",
      "WebSearch",
      "Bash(curl -s *)",
      "Bash(curl -L *)",
      "Bash(~/bin/xhs-ocr *)",
      "Bash(ssh-keyscan *)"
    ]
  }
}
